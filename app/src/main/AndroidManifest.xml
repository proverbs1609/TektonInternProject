<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.hamang.tektonproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_dogtionary"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_dogtionary_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />

        <activity android:name=".InfoActivity"></activity>
        <activity android:name=".EmotionActivity.EmotionMypage" />
        <activity android:name=".ActionActivity.ActionMypage" />
        <activity
            android:name=".SpeciesMypage"
            android:label="@string/title_activity_species_mypage"
            android:theme="@style/AppTheme" />

        <!-- [naveroauthlogin][start] 네이버아이디로로그인을 사용하기 위해서 넣어줘야하는 부분 -->
        <activity android:name="com.nhn.android.naverlogin.ui.OAuthLoginActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"/>
        <activity android:name="com.nhn.android.naverlogin.ui.OAuthLoginInAppBrowserActivity"
            android:label="OAuth2.0 In-app"/>
        <!-- [naveroauthlogin][end] -->
    </application>
</manifest>